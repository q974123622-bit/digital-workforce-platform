FROM node:22-slim

RUN npm install -g @deepseek-ai/dsh@0.1.0-rc.6

COPY docker/dsh-profile /opt/dwp/profile

# The platform profile disables shell, filesystem, workflow and subagent tools.
ENV DSH_PERMISSION_MODE=read-only

ENTRYPOINT ["dsh"]
